class NavigationModel {
  int? distanceValue;
  int? durationValue;

  String? eText;
  String? distanceText;
  String? durationText;

  NavigationModel(this.distanceValue, this.durationValue, this.eText,
      this.distanceText, this.durationText);
}
